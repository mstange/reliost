# Tell the App Engine to use the Dockerfile in the current directory.
runtime: custom
env: flex

# Let's use the basic scaling with 1 instance and 10 minutes idle timeout for now.
# We need to remove this once we really want to deploy for production.
basic_scaling:
  max_instances: 1
  idle_timeout: 10m
